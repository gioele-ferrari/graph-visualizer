{
  "name": "graph-visualizer",
  "version": "1.0.0",
  "description": "Graph visualizer made in TypeScript",
  "main": "dist/bundle.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --format=esm",
    "start": "npx http-server -c-1"
  },
  "devDependencies": {
    "esbuild": "^0.18.15",
    "typescript": "^5.6.3"
  }
}